import { VerticalBox, Button } from "std-widgets.slint";

export component WelcomePage {
    callback start-clicked();
    
    VerticalBox {
        alignment: center;
        
        Text {
            text: "Photo Viewer";
            font-size: 48px;
            font-weight: 700;
            horizontal-alignment: center;
        }
        
        Text {
            text: "photo viewer architecture demo";
            font-size: 16px;
            color: #666;
            horizontal-alignment: center;
        }
        
        Rectangle { height: 40px; }
        
        Button {
            text: "Open Album";
            primary: true;
            clicked => { start-clicked(); }
        }
        
        Rectangle { height: 20px; }
        
        Text {
            text: "Click to browse for a folder containing photos and start the demo";
            font-size: 12px;
            color: #999;
            horizontal-alignment: center;
        }
    }
}

